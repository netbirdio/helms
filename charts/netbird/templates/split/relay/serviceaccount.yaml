{{- if .Values.backend.split.relay.serviceAccount.create }}
{{- $values := .Values.backend.split.relay -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "netbird.serviceAccountName" (dict "root" . "component" "relay" "values" $values) }}
  namespace: {{ include "netbird.namespace" . }}
  labels:
    {{- include "netbird.labels" (dict "root" . "component" "relay") | nindent 4 }}
  {{- with $values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
